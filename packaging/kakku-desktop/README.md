# kakku-desktop

Meta-package for the default Kakku OS desktop package set.

Build from this directory after `kakku-hyprland-settings` is available locally or in a configured package repository:

```bash
makepkg -si
```

