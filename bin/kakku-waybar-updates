#!/usr/bin/env bash
set -euo pipefail

if ! command -v checkupdates >/dev/null 2>&1; then
  echo "updates ?"
  exit 0
fi

count="$(checkupdates 2>/dev/null | wc -l)"

if [[ "$count" -eq 0 ]]; then
  echo "up to date"
else
  echo "$count updates"
fi

